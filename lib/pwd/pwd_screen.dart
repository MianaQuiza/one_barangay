import 'package:flutter/material.dart';

class PwdScreen extends StatelessWidget {
  const PwdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PWD Services')),
      body: const Center(child: Text('PWD services information will appear here.')),
    );
  }
}
